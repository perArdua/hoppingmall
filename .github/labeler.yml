feat:
  changed-files:
    any:
      - "module-*/**/*.kt"
      - "**/application-*.yml"

fix:
  changed-files:
    any:
      - "*/**/*.kt"
      - "**/*.md"

refactor:
  changed-files:
    any:
      - "*/**/*.kt"

test:
  changed-files:
    any:
      - "*/**/test/**/*.kt"
      - "**/test/**"

docs:
  changed-files:
    any:
      - "**/*.md"
      - "README.md"

chore:
  changed-files:
    any:
      - "**/.gitignore"
      - "**/build.gradle.kts"
      - "**/settings.gradle.kts"
